// components/topics/CssSelectors.tsx
const CssSelectors = () => {
    return (
        <div>
            <h2>CSS Selectors</h2>
            <p>This is the content for CSS Selectors.</p>
        </div>
    );
};

export default CssSelectors;
